import React from 'react';

const Tokens = () => {
  return (
    <div className="page tokens-page">
      <h2>Tokens</h2>
      <p className="text-muted">Manage API tokens / access keys. (Placeholder page)</p>
    </div>
  );
};

export default Tokens;
