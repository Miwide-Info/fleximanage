import React from 'react';

const AccountAccessKeys = () => (
  <div className="page account-access-keys">
    <h2>Access Keys</h2>
    <p>Generate and manage API access keys for automation.</p>
  </div>
);

export default AccountAccessKeys;
